var x = 71;
var y = 49;
var r = 0;
var side = 36;
for( var i=1; i<=7; i++) {
    rect(x, y, side, side, r);
    x = x + side;
}


var x = 71;
var y = y + side;
for( var i=1; i<=7; i++) {
    rect(x, y, side, side, r);
    x = x + side;
}

// 3rd row
var x = 71;
var y = y + side;
for(var i=1; i<=7; i++) {
    rect(x, y, side, side, r);
    x = x + side;
}

var x = 71;
var y = y + side;
for( var i=1; i<=7; i++) {
    rect(x, y, side, side, r);
    x = x + side;
}

var x = 71;
var y = y + side;
for( var i=1; i<=3; i++) {
    rect(x, y, side, side, r);
    x = x + side;
}


